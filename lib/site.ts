// Which memorial this deployment serves. All three sites share one Supabase
// project; every row is tagged with this slug so each site reads and writes
// only its own tributes and gallery photos.
export const SITE_SLUG = 'mumbi';
